using System;
using System.Collections.Generic;
using System.Linq;
using PerftEvaluation.DTO;

namespace PerftEvaluation.BAL.Interfaces {
    public interface IFilterExtensions {
        // static IEnumerable<T> Filter<T> (this IEnumerable<T> collection, List<string> columnNames, string searchText, bool isAndCondition = false);
        // static IQueryable<T> SortAndFilter<T> (this IQueryable<T> collection, RequestModel requestModel, List<string> filterColumns = null);
        // staticIQueryable<T> OrderBy<T> (this IQueryable<T> collection, string attribute, bool IsDescending);
    }
}